// The theme ships no Go code — this file exists so it can be consumed as a Hugo
// Module. The `go` directive is kept low on purpose: it only gates the toolchain
// a consumer needs, and there is nothing here that requires a recent one.
module github.com/chidokun/hugo-blog-tuan

go 1.19
